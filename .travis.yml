language: python
python:
  - "3.10"

matrix:
  include:
    - env: TOXENV=django-3.2
    - env: TOXENV=django-4.2
    - env: TOXENV=django-5.0
    - env: TOXENV=django-5.2
    - env: TOXENV=coverage

install:
  - pip install tox

script: tox
